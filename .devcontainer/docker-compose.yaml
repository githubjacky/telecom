services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace/telecom
    # this command is referenced to https://github.com/loft-sh/devpod/issues/955#issue-2177966943
    command: /bin/sh -c "trap :; TERM INT; sleep infinity & wait"
